name: Publish prerelease
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  publish:
    uses: observeinc/.github/.github/workflows/terraform-observe_prerelease.yaml@arthur/arthur/repo-override
    secrets: inherit
    with:
      repo-name-slack: "terraform-observe-google"
