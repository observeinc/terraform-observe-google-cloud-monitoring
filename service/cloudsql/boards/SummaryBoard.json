{
  "bindings": { 
    "dataset_metrics": "${dataset_metrics}",
    "dataset_resource": "${dataset_resource}"
  },
  "sections": [
    ${SUMMARY_SECTION},
    ${METRICS_SECTION},
    ${DOC_SECTION}
  ]
}
